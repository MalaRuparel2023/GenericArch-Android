# INDEX — what THIS repo has

`MAP.tsv` is what the base *provides*. This file is what this product *has*. A thing in neither is a
thing nobody finds.

In an adopting repo this file lists that product's modules, features, flavours and integrations,
generated by `/project-init` and kept current by `/sync-app-notes`.

**In this repo — the base itself — there is no product.** What exists:

| | State |
|---|---|
| Rules (`CLAUDE.md` §0–§12, 18 §2 rules) | written |
| Docs skeleton (11 files + two templates) | written |
| Router (`MAP.tsv`) | written, 43 rows |
| Skills (`new-feature`, `debug`) | written |
| Commands (12) | written |
| Design note (`docs/DESIGN-NOTE.md`) | written — the source of truth for everything above |
| `androidArch*` Gradle tasks | **not built** — `docs/GAPS.md` row 1 |
| `TASKS.tsv`, install manifest, CI, generated notes | **not built** — `docs/GAPS.md` rows 2–5 |

Build order and what each step is worth: `docs/DESIGN-NOTE.md` §12.
